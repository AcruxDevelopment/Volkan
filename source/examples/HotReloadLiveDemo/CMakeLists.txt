# See source/examples/HotReloadDemo/CMakeLists.txt for why this
# grouping file needs to exist at all (one-level-deep discovery in
# cmake/BuildHelpers.cmake).
add_subdirectory(Plugin)
add_subdirectory(Host)
